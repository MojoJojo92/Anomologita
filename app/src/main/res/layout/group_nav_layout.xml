<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="7dp"
    android:background="@android:color/transparent"
    android:clickable="true">

    <ImageView
        android:id="@+id/circle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_margin="5dp"
        android:contentDescription="@string/bigCircleDesc"
        android:src="@drawable/ic_background_circle_small"/>

    <ImageView
        android:id="@+id/icon"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_centerVertical="true"
        android:layout_margin="5dp"
        android:contentDescription="@string/bigCircleDesc"
        android:src="@drawable/ic_background_circle_small"/>

    <TextView
        android:id="@+id/groupNameNav"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_centerInParent="true"
        android:layout_toEndOf="@+id/icon"
        android:layout_toLeftOf="@+id/subs"
        android:layout_toRightOf="@id/icon"
        android:layout_toStartOf="@id/subs"
        android:gravity="start"
        android:text="@string/groupName"
        android:textColor="@color/white"/>

    <LinearLayout
        android:id="@+id/subs"
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_centerInParent="true"
        android:layout_margin="5dp"
        android:background="@drawable/subscribe_background"
        android:orientation="horizontal"
        android:padding="2dp">

        <TextView
            android:id="@+id/subCount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@android:color/transparent"
            android:drawableLeft="@drawable/ic_star_white"
            android:drawableStart="@drawable/ic_star_white"
            android:maxLines="1"
            android:padding="1dp"
            android:text="@string/subs"
            android:textColor="@color/white"/>

    </LinearLayout>

</RelativeLayout>